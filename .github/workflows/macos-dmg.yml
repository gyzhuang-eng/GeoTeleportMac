name: macos-dmg

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "GeoTeleportMac/**"
      - "GeoTeleportMac.xcodeproj/**"
      - "native-device-core/**"
      - "build_dmg.sh"
      - ".github/workflows/macos-dmg.yml"
  push:
    branches:
      - "v3/windows-host"
      - "v3/device-core-rust"
    paths:
      - "GeoTeleportMac/**"
      - "GeoTeleportMac.xcodeproj/**"
      - "native-device-core/**"
      - "build_dmg.sh"
      - ".github/workflows/macos-dmg.yml"

jobs:
  build:
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - name: Show toolchain versions
        run: |
          sw_vers
          xcodebuild -version
          rustc --version
          cargo --version

      - name: Build unsigned DMG
        run: ./build_dmg.sh

      - name: Upload unsigned DMG
        uses: actions/upload-artifact@v4
        with:
          name: GeoTeleportMacV3-unsigned-dmg
          path: GeoTeleportMacV3.dmg
          if-no-files-found: error
